diff --git a/labels.py b/labels.py
--- a/labels.py
+++ b/labels.py
@@ -132,7 +132,10 @@ class Label:
         self.image = self.idle_image
         self.clicked = False
         self.hover = False
+    def __init__(self, font, font_size, disable_image=None):
+        super().__init__()
+        self.font = font
+        self.font_size = font_size
+        if disable_image is not None:
+            self.disable_image = disable_image
     def render_text(self):
         font, size = self.font, self.font_size
         if (font, size) not in LOADED_FONTS:
             LOADED_FONTS[font, size] = pg.font.Font(font, size)