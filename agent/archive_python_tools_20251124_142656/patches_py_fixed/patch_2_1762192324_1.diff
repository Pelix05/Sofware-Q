diff --git a/labels.py b/labels.py
--- a/labels.py
+++ b/labels.py
@@ -132,7 +132,9 @@ class Label:
         self.clicked = False
         self.hover = False
     def render_text(self):
-        font, size = self.font, self.font_size
+        font, size = self.font, self.font_size
         if (font, size) not in LOADED_FONTS:
             LOADED_FONTS[font, size] = pg.font.Font(font, size)
         self.font = LOADED_FONTS[font, size]
         text = self.text and self.font.render(self.text, 1, self.text_color)
+        # Fixing the possibly-used-before-assignment issue
+        x_diff, y_diff = self.get_position_difference()
         hover = self.hover_text and self.font.render(self.hover_text, 1,
                                                 self.hover_color)
@@ -139,6 +141,8 @@ class Label:
         x, y = self.position
         x_diff, y_diff = abs(x - self.x), abs(y - self.y)
+        self.x_diff = x_diff
+        self.y_diff = y_diff
         return text, hover