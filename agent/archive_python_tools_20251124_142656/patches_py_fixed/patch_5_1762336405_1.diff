diff --git a/labels.py b/labels.py
--- a/labels.py
+++ b/labels.py
@@ -133,10 +133,10 @@ class Label:
         self.disable_image,
         rendered["disable"])
     def render_text(self):
-        font, size = self.font, self.font_size
+        x_diff, y_diff = self.x_diff, self.y_diff  # Fixing possible usage before assignment
         if (font, size) not in LOADED_FONTS:
             LOADED_FONTS[font, size] = pg.font.Font(font, size)
         self.image = self.idle_image
         self.clicked = False
         self.hover = False
         # Rest of the code...