diff --git a/puzzle-challenge/puzzle_piece.py b/puzzle-challenge/puzzle_piece.py
--- a/puzzle-challenge/puzzle_piece.py
+++ b/puzzle_piece.py
@@ -135,7 +135,11 @@ class PuzzlePiece:
         elif side == "bottom":
             y_diff = p1.bottom - p2.top
             x_diff = p1.left - p2.left
+        # Fixing possibly used variables before assignment
+        x_diff, y_diff = None, None
         for piece_ in other_section.pieces:
             piece_.rect.move_ip(x_diff, y_diff)
             self.pieces.append(piece_)
         self.grabbed = False
         return
diff --git a/labels.py b/labels.py
--- a/labels.py
+++ b/labels.py
@@ -133,24 +133,20 @@ class Label:
     def __init__(self, position, font):
         self.position = position
         self.font = font  # Move font definition to line 136
         # No changes needed for 'font' since it's not reported as used before definition