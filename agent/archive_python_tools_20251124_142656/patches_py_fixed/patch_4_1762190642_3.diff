diff --git a/puzzle-challenge/puzzle_piece.py b/puzzle-challenge/puzzle_piece.py
--- a/puzzle-challenge/puzzle_piece.py
+++ b/puzzle_piece.py
@@ -137,20 +137,20 @@ def grab_piece(side, p1, p2, other_section):
         elif side == "bottom":
             y_diff = p1.bottom - p2.top
             x_diff = p1.left - p2.left
         for piece_ in other_section.pieces:
             piece_.rect.move_ip(x_diff, y_diff)
             self.pieces.append(piece_)
         self.grabbed = False
         return
     def get_event(self, event):
         if self.grabbed:
-            x_diff, y_diff = self.rect.size
+            x_diff = self.x_diff  # Fixing 'x_diff' assignment
+            y_diff = self.y_diff  # Fixing 'y_diff' assignment
             piece_to_release = self.pieces.pop(0)
             piece_to_release.rect.move_ip(-x_diff, -y_diff)
         else:
             super().get_event(event)