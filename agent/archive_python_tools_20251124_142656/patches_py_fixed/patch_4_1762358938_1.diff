diff --git a/labels.py b/labels.py
--- a/labels.py
+++ b/labels.py
@@ -132,7 +132,7 @@ class Label:
         self.disable_image, rendered["disable"]):
             self.image = self.idle_image
             self.clicked = False
             self.hover = False
-        self.render_text()
+        self.render_text(font=self.font, size=self.font_size)
    def render_text(self, font=None, size=None):
        if font is not None and size is not None:
            font, _ = font, size  # Use only one argument for consistency