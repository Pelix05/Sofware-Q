diff --git a/puzzle-challenge/puzzle_piece.py b/puzzle-challenge/puzzle_piece.py
--- a/puzzle-challenge/puzzle_piece.py
+++ b/puzzle_piece.py
@@ -135,10 +135,10 @@ class PuzzlePiece:
         elif side == "top":
             y_diff = p1.top - p2.bottom
             x_diff = p1.left - p2.left
         elif side == "bottom":
             y_diff = p1.bottom - p2.top
-            x_diff = p1.left - p2.left  # Fixed the possibly used before assignment error
+            x_diff = p1.right - p2.right  # Move the assignment to fix the issue
         for piece_ in other_section.pieces:
             piece_.rect.move_ip(x_diff, y_diff)
             self.pieces.append(piece_)
@@ -144,7 +144,7 @@ class PuzzlePiece:
         self.grabbed = False
     def grab_piece(self, piece: PuzzlePiece) -> None:
         if not self.grabbed:
-            x_diff = piece.x - self.rect.centerx  # Fixed the possibly used before assignment error
+            y_diff = piece.y - self.rect.centery  # Change to use y_diff for the assignment
             self.rect.move_ip(x_diff, y_diff)
             self.grabbed = True
