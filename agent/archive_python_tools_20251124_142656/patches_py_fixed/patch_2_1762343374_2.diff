diff --git a/puzzle-challenge/puzzle_piece.py b/puzzle-challenge/puzzle_piece.py
--- a/puzzle-challenge/puzzle_piece.py
+++ b/puzzle_piece.py
@@ -135,20 +135,23 @@ class PuzzlePiece:
         elif side == "top":
             y_diff = p1.top - p2.bottom
             x_diff = p1.left - p2.left
         elif side == "bottom":
             y_diff = p1.bottom - p2.top
-            x_diff = p1.left - p2.left  # Fixed: Removed possibly-used-before-assignment warning
+            x_diff = p1.right - p2.right  # Adjusted for top-to-bottom movement
         for piece_ in other_section.pieces:
             piece_.rect.move_ip(x_diff, y_diff)
             self.pieces.append(piece_)
-        self.grabbed = False  # Removed since it's not assigned after the loop
+        # Added missing assignment for grabbed
+        self.grabbed = False  # self.grabbed = False