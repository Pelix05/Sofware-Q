diff --git a/puzzle-challenge/puzzle_piece.py b/puzzle-challenge/puzzle_piece.py
--- a/puzzle-challenge/puzzle_piece.py
+++ b/puzzle_piece.py
@@ -135,7 +135,9 @@ class PuzzlePiece:
         elif side == "top":
             y_diff = p2.top - p1.bottom
             x_diff = p2.left - p1.left
         else:
-            x_diff = p1.bottom - p2.top
-            y_diff = p1.left - p2.left
+            x_diff = p1.bottom - p2.top
+            y_diff = p2.left - p1.left
         for piece_ in other_section.pieces:
             piece_.rect.move_ip(x_diff, y_diff)
             self.pieces.append(piece_)
         self.grabbed = False
         return
@@ -143,7 +145,7 @@ class PuzzlePiece:
     def get_event(self, event):
         if event.type == pygame.MOUSEBUTTONDOWN and self.grabbed:
             # Handle click event
         elif event.type == pygame.MOUSEBUTTONUP and not self.grabbed:
-            x, y = event.pos
+            x, y = event.pos
             self.drop_at(x, y)
         # Other event handling...
