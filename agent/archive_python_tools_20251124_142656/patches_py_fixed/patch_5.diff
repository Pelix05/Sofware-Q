diff --git a/labels.py b/labels.py
--- a/labels.py
+++ b/labels.py
@@ -131,8 +131,9 @@ class Label:
         self.font, self.font_size = font, size
     def render_text(self):
         if (font, size) not in LOADED_FONTS:
             LOADED_FONTS[font, size] = pg.font.Font(font, size)
-        self.font = LOADED_FONTS[font, size]
+        self.font = LOADED_FONTS.get(font, size, fallback=pg.font.Font(font, size))
+        self.hover_font = LOADED_FONTS.get(self.hover_font_name, self.font, fallback=self.font)
         text = self.text and self.font.render(self.text, 1, self.text_color)
         hover = self.hover_text and self.font.render(self.hover_text, 1,
             color=self.hover_color if self.hover else None)
